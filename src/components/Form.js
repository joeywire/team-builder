import React from 'react';
import '../App.css';

function Form(props) {

    return (
        <div className="Form">
            <h2>Team Builder Form</h2>
        </div>
    );
}

export default Form;